syntax = "proto3";
package codes.rik.window.post.api;

message CreatePostRequest {
    string userId = 1; // debugging
}

message CreatePostResponse {
    string postId = 1;
    string uploadUrl = 2;
    uint64 uploadExpirationTime = 3;
}
